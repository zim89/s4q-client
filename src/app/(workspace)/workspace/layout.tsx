import { WorkspaceLayout } from '@/screens/workspace'

const Layout = ({ children }: { children: React.ReactNode }) => {
  return <WorkspaceLayout>{children}</WorkspaceLayout>
}

export default Layout
